'use strict';


// Declare app level module which depends on filters, and services
var app = angular.module('app', [
  'ui.router',
  'ngAnimate',
  'app.services',
  'app.filters',
  'app.controllers',
  'app.directives'
]);

app.config(function(){
    
});
